---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: OCIRepository
metadata:
  name: teleport-tbot
  namespace: giantswarm
spec:
  interval: 10m
  provider: generic
  ref:
    semver: x.x.x
  url: oci://gsoci.azurecr.io/charts/giantswarm/teleport-tbot
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: teleport-tbot
  namespace: giantswarm
  annotations:
    app-operator.giantswarm.io/paused: 'true'
spec:
  chartRef:
    kind: OCIRepository
    name: teleport-tbot
    namespace: giantswarm
  install:
    remediation:
      remediateLastFailure: false
      retries: 10
  interval: 5m
  releaseName: teleport-tbot
  targetNamespace: giantswarm
  timeout: 10m
  upgrade:
    remediation:
      remediateLastFailure: false
      retries: 10
  valuesFrom:
  - kind: ConfigMap
    name: teleport-tbot-konfiguration
    valuesKey: configmap-values.yaml
  - kind: Secret
    name: teleport-tbot-konfiguration
    valuesKey: secret-values.yaml
