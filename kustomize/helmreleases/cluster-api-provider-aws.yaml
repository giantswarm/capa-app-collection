---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: OCIRepository
metadata:
  name: cluster-api-provider-aws
  namespace: giantswarm
spec:
  interval: 10m
  provider: generic
  ref:
    semver: x.x.x
  url: oci://gsoci.azurecr.io/charts/giantswarm/cluster-api-provider-aws
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cluster-api-provider-aws
  namespace: giantswarm
  annotations:
    app-operator.giantswarm.io/paused: 'true'
spec:
  chartRef:
    kind: OCIRepository
    name: cluster-api-provider-aws
    namespace: giantswarm
  install:
    remediation:
      remediateLastFailure: false
      retries: 10
  interval: 5m
  releaseName: cluster-api-provider-aws
  targetNamespace: giantswarm
  timeout: 10m
  upgrade:
    remediation:
      remediateLastFailure: false
      retries: 10
  valuesFrom:
  - kind: ConfigMap
    name: cluster-api-provider-aws-konfiguration
    valuesKey: configmap-values.yaml
  - kind: Secret
    name: cluster-api-provider-aws-konfiguration
    valuesKey: secret-values.yaml
