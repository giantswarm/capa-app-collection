---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: OCIRepository
metadata:
  name: prometheus-remotewrite
  namespace: giantswarm
spec:
  interval: 10m
  provider: generic
  ref:
    semver: x.x.x
  url: oci://gsoci.azurecr.io/charts/giantswarm/prometheus-remotewrite
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: prometheus-remotewrite
  namespace: giantswarm
  annotations:
    app-operator.giantswarm.io/paused: 'true'
spec:
  chartRef:
    kind: OCIRepository
    name: prometheus-remotewrite
    namespace: giantswarm
  install:
    remediation:
      remediateLastFailure: false
      retries: 10
  interval: 5m
  releaseName: prometheus-remotewrite
  targetNamespace: giantswarm
  timeout: 10m
  upgrade:
    remediation:
      remediateLastFailure: false
      retries: 10
  valuesFrom:
  - kind: ConfigMap
    name: prometheus-remotewrite-konfiguration
    valuesKey: configmap-values.yaml
  - kind: Secret
    name: prometheus-remotewrite-konfiguration
    valuesKey: secret-values.yaml
